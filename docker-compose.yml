version: "3.9"

services:
  kafka:
    image: bitnami/kafka:3.7
    environment:
      KAFKA_CFG_NODE_ID: "0"
      KAFKA_CFG_PROCESS_ROLES: controller,broker
      KAFKA_CFG_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_CFG_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CFG_CONTROLLER_QUORUM_VOTERS: 0@kafka:9093
      KAFKA_CFG_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CFG_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_CFG_NUM_PARTITIONS: "4"
      KAFKA_CFG_DEFAULT_REPLICATION_FACTOR: "1"
      KAFKA_CFG_LOG_RETENTION_HOURS: "2"
    ports:
      - "9092:9092"
    healthcheck:
      test: ["CMD-SHELL", "kafka-topics.sh --bootstrap-server localhost:9092 --list"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 30s

  etcd:
    image: gcr.io/etcd-development/etcd:v3.5.14
    command:
      - /usr/local/bin/etcd
      - --name=etcd0
      - --data-dir=/etcd-data
      - --listen-client-urls=http://0.0.0.0:2379
      - --advertise-client-urls=http://etcd:2379
      - --listen-peer-urls=http://0.0.0.0:2380
      - --initial-advertise-peer-urls=http://etcd:2380
      - --initial-cluster=etcd0=http://etcd:2380
      - --initial-cluster-token=air-quality
      - --initial-cluster-state=new
    ports:
      - "2379:2379"
    volumes:
      - etcd-data:/etcd-data
    healthcheck:
      test: ["/usr/local/bin/etcdctl", "endpoint", "health"]
      interval: 10s
      timeout: 5s
      retries: 5

  nats:
    image: nats:2.10-alpine
    ports:
      - "4222:4222"
      - "8222:8222"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8222/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5

  collector-1:
    build:
      context: .
      dockerfile: docker/Dockerfile.collector
    environment:
      ETCD_ENDPOINTS: http://etcd:2379
      NATS_URL: nats://nats:4222
      OPENAQ_API_KEY: ${OPENAQ_API_KEY:-}
      FETCH_INTERVAL: ${FETCH_INTERVAL:-5m}
      WINDOW_DURATION: ${WINDOW_DURATION:-60s}
      WINDOW_MAX_SIZE: ${WINDOW_MAX_SIZE:-500}
      PUBLISH_RAW: "true"
      INSTANCE_ID: collector-1
      METRICS_ADDR: ":8080"
      FLIGHT_ADDR: ":5005"
      FLIGHT_STORE_LEN: "200"
      KAFKA_BROKERS: kafka:9092
    ports:
      - "8081:8080"   # metrics/shards HTTP
      - "5005:5005"   # Arrow Flight gRPC (primary)
    depends_on:
      etcd:
        condition: service_healthy
      nats:
        condition: service_healthy
      kafka:
        condition: service_healthy

  collector-2:
    build:
      context: .
      dockerfile: docker/Dockerfile.collector
    environment:
      ETCD_ENDPOINTS: http://etcd:2379
      NATS_URL: nats://nats:4222
      OPENAQ_API_KEY: ${OPENAQ_API_KEY:-}
      FETCH_INTERVAL: ${FETCH_INTERVAL:-5m}
      WINDOW_DURATION: ${WINDOW_DURATION:-60s}
      WINDOW_MAX_SIZE: ${WINDOW_MAX_SIZE:-500}
      PUBLISH_RAW: "true"
      INSTANCE_ID: collector-2
      METRICS_ADDR: ":8080"
      FLIGHT_ADDR: ":5005"
      FLIGHT_STORE_LEN: "200"
    ports:
      - "8082:8080"
      - "5006:5005"   # Arrow Flight gRPC
    depends_on:
      etcd:
        condition: service_healthy
      nats:
        condition: service_healthy

  collector-3:
    build:
      context: .
      dockerfile: docker/Dockerfile.collector
    environment:
      ETCD_ENDPOINTS: http://etcd:2379
      NATS_URL: nats://nats:4222
      OPENAQ_API_KEY: ${OPENAQ_API_KEY:-}
      FETCH_INTERVAL: ${FETCH_INTERVAL:-5m}
      WINDOW_DURATION: ${WINDOW_DURATION:-60s}
      WINDOW_MAX_SIZE: ${WINDOW_MAX_SIZE:-500}
      PUBLISH_RAW: "true"
      INSTANCE_ID: collector-3
      METRICS_ADDR: ":8080"
      FLIGHT_ADDR: ":5005"
      FLIGHT_STORE_LEN: "200"
    ports:
      - "8083:8080"
      - "5007:5005"   # Arrow Flight gRPC
    depends_on:
      etcd:
        condition: service_healthy
      nats:
        condition: service_healthy

  analysis-api:
    build:
      context: .
      dockerfile: docker/Dockerfile.analysis
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      NATS_URL: nats://nats:4222
      FLIGHT_ENDPOINT: grpc://collector-1:5005
      KAFKA_BROKERS: kafka:9092
    ports:
      - "8000:8000"
    depends_on:
      nats:
        condition: service_healthy
      kafka:
        condition: service_healthy
      collector-1:
        condition: service_started

  dashboard:
    build:
      context: .
      dockerfile: docker/Dockerfile.analysis
    command: streamlit run dashboard/app.py --server.port=8501 --server.address=0.0.0.0
    environment:
      API_URL: http://analysis-api:8000
    ports:
      - "8501:8501"
    depends_on:
      - analysis-api

volumes:
  etcd-data:
